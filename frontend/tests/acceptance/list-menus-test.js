import { test } from 'qunit';
import moduleForAcceptance from 'frontend/tests/helpers/module-for-acceptance';

moduleForAcceptance('Acceptance | list menus');

test('should show specials and offers on the home page', function (assert) {
});

test('should link to information about the restaurant.', function (assert) {
});

test('should link to contact information.', function (assert) {
});

test('should list food items.', function (assert) {
});

test('should show food items to checkout and social media login.', function (assert) {
});

test('should show payment details', function (assert) {
});


/* test('visiting /', function(assert) {
  visit('/');

  andThen(function() {
    assert.equal(currentURL(), '/');
  });
}); */
